<%= form_for(@mailee_list) do |f| %>
  <% if @mailee_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailee_list.errors.count, "error") %> prohibited this mailee_list from being saved:</h2>

      <ul>
      <% @mailee_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <% for field in [:name, :company, :description, :address, :phone, :site] %>
      <%= f.label field %><br />
      <%= f.text_field field %>
      <br />
    <% end %>
  </div>

 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
